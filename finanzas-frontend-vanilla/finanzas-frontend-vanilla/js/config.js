// BASE_URL de la API (ej: http://localhost:3000/api). Lee de un valor fijo o de una variable global seteada al hacer build/deploy
// Si en algún momento subís el backend a un servidor, este es el único lugar que hay que cambiar.
export const API_BASE_URL = 'http://localhost:3000/api';